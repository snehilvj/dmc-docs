import dash_mantine_components as dmc
from lib.configurator import create_configurator

component = dmc.TextInput(label="Full name", placeholder="Your name")

# component = create_configurator(demo, controls)
