Anchor | Display links with theme styles.

### Simple Example

dmc.Anchor is a wrapper around dmc.Text component with root component: `a`. It takes the same props as dmc.Text.

> example:components/anchor/_simple.py

> apidoc:Anchor